# create CRUD pdo

- php data object

```bash
CREATE TABLE users(
    ID INT AUTO_INCREMENT PRIMARY KEY,
    fname VARCHAR(255),
    lname VARCHAR(255),
    email VARCHAR(255),
    phone VARCHAR(255)
);
```